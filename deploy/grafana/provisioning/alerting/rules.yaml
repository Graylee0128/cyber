apiVersion: 1

# 唯一 alert engine（ADR ③）。eval interval 10s（spec §1、票 03）。
# SQLi burst：對 Loki 數 sqli_suspected=true 的筆數，> 0 就 firing。
groups:
  - orgId: 1
    name: purple-detection
    folder: PurpleScope
    interval: 10s
    rules:
      - uid: sqli-injection-burst
        title: SQLInjectionBurst
        condition: C
        for: 0s
        noDataState: OK
        execErrState: Error
        data:
          - refId: A
            relativeTimeRange:
              from: 60
              to: 0
            datasourceUid: loki
            model:
              refId: A
              datasource:
                type: loki
                uid: loki
              # 以 source_ip 分 series，Grafana webhook 才會把實際攻擊來源帶進 Core Event；
              # response agent 只執行這個已治理的值，不自行猜測封鎖對象（#17）。
              #
              # 注意：falco-command-exec 有同款 by(source_ip) 分組，實測改成
              # queryType: instant 才能讓多來源同時 firing 時各自形成獨立 alert
              # instance（見 #17 commit bea5f2c）。這條規則刻意**沒有**跟著改──
              # 改了會讓這條規則在攻擊停止後於 60s 老化窗內反覆 firing/resolved
              # 閃動，每次閃動都重新 enqueue 一次封鎖指令（ingest_alert 的 enqueue
              # 不看 CoreEventStore 是否去重），T2 的
              # test_sqli_alert_resolves_after_attack_stops 因此觀察到 5 次
              # response.executed 而非 1 次、resolved 事件也對不上原本的 firing
              # event_id。目前沒有測試要求 SQLi 場景要撐多來源同時攻擊，維持原本
              # range+reduce+threshold 寫法，把 instant 的修法留給真的需要它、且
              # 一併處理閃動問題的後續票。
              expr: 'sum by (source_ip) (count_over_time({app="vulnerable-app"} | json | sqli_suspected=`true` [1m]))'
              queryType: range
              intervalMs: 1000
              maxDataPoints: 43200
          - refId: B
            datasourceUid: __expr__
            model:
              refId: B
              type: reduce
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              reducer: last
          - refId: C
            datasourceUid: __expr__
            model:
              refId: C
              type: threshold
              datasource:
                type: __expr__
                uid: __expr__
              expression: B
              conditions:
                - evaluator:
                    type: gt
                    params: [0]
        labels:
          event_type: attack.detected
          technique: T1190
          team: red
          severity: high
          scenario_id: sqli-01
          service: vulnerable-app
        annotations:
          query: '{app="vulnerable-app"} | json | sqli_suspected=`true`'
          threshold: "> 0 / 1m"

      # range-target `/product`（票 #44 的計分攻擊面）：#47 卡點 —— 這條規則之前完全不存在，
      # 靶機真攻擊面唯一沒接偵測的技法。判定欄位 `sqli_suspected` 由 app.py 算好（同款
      # `_looks_like_sqli`，比照 vulnerable-app），規則只查欄位，跟上面那條分工一致。
      #
      # scenario_id 先用 `range-target-sqli` 佔位 —— #47 的 metadata.yaml 還沒定案真正
      # scenario id，等那張票選定 id 後這裡要跟著改，否則 Core Event 的 scenario_id
      # 對不上 scenario 檔，P2 那邊會找不到對應的 scenario。
      - uid: range-target-sqli-injection
        title: RangeTargetSQLInjection
        condition: C
        for: 0s
        noDataState: OK
        execErrState: Error
        data:
          - refId: A
            relativeTimeRange:
              from: 60
              to: 0
            datasourceUid: loki
            model:
              refId: A
              datasource:
                type: loki
                uid: loki
              expr: 'sum by (source_ip) (count_over_time({app="range-target"} | json | path=`/product` | sqli_suspected=`true` [1m]))'
              queryType: range
              intervalMs: 1000
              maxDataPoints: 43200
          - refId: B
            datasourceUid: __expr__
            model:
              refId: B
              type: reduce
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              reducer: last
          - refId: C
            datasourceUid: __expr__
            model:
              refId: C
              type: threshold
              datasource:
                type: __expr__
                uid: __expr__
              expression: B
              conditions:
                - evaluator:
                    type: gt
                    params: [0]
        labels:
          event_type: attack.detected
          technique: T1190
          team: red
          severity: high
          scenario_id: range-target-sqli
          service: range-target
        annotations:
          query: '{app="range-target"} | json | path=`/product` | sqli_suspected=`true`'
          threshold: "> 0 / 1m"

      # Brute force：走 metric（PromQL）而非 log。1 分鐘內失敗登入增量 > 10 才 firing，
      # 讓少量正常請求不會誤觸（負向 E2E 靠這個門檻不被 brute force 蓋過）。
      - uid: ssh-brute-force
        title: SSHBruteForce
        condition: C
        for: 0s
        noDataState: OK
        execErrState: Error
        data:
          - refId: A
            relativeTimeRange:
              from: 60
              to: 0
            datasourceUid: prometheus
            model:
              refId: A
              datasource:
                type: prometheus
                uid: prometheus
              expr: 'sum(increase(ssh_failed_logins_total[1m]))'
              instant: true
              range: false
              intervalMs: 1000
              maxDataPoints: 43200
          - refId: B
            datasourceUid: __expr__
            model:
              refId: B
              type: reduce
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              reducer: last
          - refId: C
            datasourceUid: __expr__
            model:
              refId: C
              type: threshold
              datasource:
                type: __expr__
                uid: __expr__
              expression: B
              conditions:
                - evaluator:
                    type: gt
                    params: [10]
        labels:
          event_type: attack.detected
          technique: T1110
          team: red
          severity: medium
          scenario_id: bruteforce-01
          service: vulnerable-app
        annotations:
          query: 'sum(increase(ssh_failed_logins_total[1m]))'
          threshold: "> 10 / 1m"

      # Falco：靶機被生出 shell 直譯器（帶 PURPLESCOPE_EXEC 標記）→ T1059。
      # Falco 只提供事實（syscall 進 Loki），是否構成告警由這條 Grafana rule 決定
      # （ADR ③：Grafana 是唯一 alert engine，Falco 不直送 Event Service）。
      - uid: falco-command-exec
        title: FalcoCommandExec
        condition: A
        for: 0s
        noDataState: OK
        execErrState: Error
        data:
          - refId: A
            relativeTimeRange:
              from: 60
              to: 0
            datasourceUid: loki
            model:
              refId: A
              datasource:
                type: loki
                uid: loki
              # range app 把 TCP 對端放進 exec marker；regexp 擷取成 source_ip series label。
              # instant query（非 range/matrix）：每個 source_ip 各自一個樣本點，
              # 才會各自成為一個 alert instance、各自帶自己的 source_ip label。
              expr: 'sum by (source_ip) (count_over_time({job="falco"} |= `PurpleScope exec detected` | regexp `SOURCE_IP=(?P<source_ip>[0-9.]+)` [1m])) > 0'
              queryType: instant
              instant: true
              range: false
              intervalMs: 1000
              maxDataPoints: 43200
        labels:
          event_type: attack.detected
          technique: T1059
          team: red
          severity: high
          scenario_id: falco-exec-01
          service: vulnerable-app
        annotations:
          query: '{job="falco"} |= `PurpleScope exec detected`'
          threshold: "> 0 / 1m"

      # SA §7 Scenario 03（Sensitive File Access）：Falco → Loki → Grafana → Incident。
      # 同樣是 Falco 只提供事實、Grafana 決定是否構成告警（ADR ③）。
      - uid: falco-sensitive-file
        title: FalcoSensitiveFileAccess
        condition: C
        for: 0s
        noDataState: OK
        execErrState: Error
        data:
          - refId: A
            relativeTimeRange:
              from: 60
              to: 0
            datasourceUid: loki
            model:
              refId: A
              datasource:
                type: loki
                uid: loki
              expr: 'sum(count_over_time({job="falco"} |= `PurpleScope sensitive file access` [1m]))'
              queryType: range
              intervalMs: 1000
              maxDataPoints: 43200
          - refId: B
            datasourceUid: __expr__
            model:
              refId: B
              type: reduce
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              reducer: last
          - refId: C
            datasourceUid: __expr__
            model:
              refId: C
              type: threshold
              datasource:
                type: __expr__
                uid: __expr__
              expression: B
              conditions:
                - evaluator:
                    type: gt
                    params: [0]
        labels:
          event_type: attack.detected
          technique: T1005
          team: red
          severity: high
          scenario_id: falco-secret-03
          service: range-target
        annotations:
          query: '{job="falco"} |= `PurpleScope sensitive file access`'
          threshold: "> 0 / 1m"
