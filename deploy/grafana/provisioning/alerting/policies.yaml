apiVersion: 1

# 全部路由到 receiver。group_wait 0s：部署測試要快，不等聚合。
policies:
  - orgId: 1
    receiver: purple-receiver
    group_by: ["alertname"]
    group_wait: 0s
    group_interval: 10s
    repeat_interval: 1h
