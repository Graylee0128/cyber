apiVersion: 1

# Grafana firing → webhook → P1 receiver。這是 D1 路徑的最後一段。
contactPoints:
  - orgId: 1
    name: purple-receiver
    receivers:
      - uid: purple-webhook
        type: webhook
        settings:
          url: http://receiver:8000/webhook
          httpMethod: POST
