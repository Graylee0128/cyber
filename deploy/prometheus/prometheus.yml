# 部署測試用 Prometheus。票 #11 起改吃 OTLP remote_write（Alloy 從 :4317 轉來），
# 不再 scrape 靶機 —— 所以沒有 scrape_configs，Prometheus 也不必上 z-target。
# remote_write receiver 由 command flag `--web.enable-remote-write-receiver` 開啟。
global:
  scrape_interval: 5s
  evaluation_interval: 5s
