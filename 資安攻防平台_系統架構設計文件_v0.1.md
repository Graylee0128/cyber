# 資安攻防平台（產品名稱待定）
## 系統架構設計文件 v0.1

- **文件版本**：v0.1
- **文件性質**：產品與系統架構初版草案
- **產品名稱**：TBD
- **目前階段**：第一階段 Software Stack / MVP 架構規劃
- **主要情境**：紅隊、藍隊、紫隊資安攻防演練
- **備註**：網路拓樸尚未定案，本文件僅保留介面與邊界，不做過度設計；後續產品預計導入 AI 輔助能力。

---

# 1. 文件目的

本文件整理目前針對「資安攻防演練平台」的產品發想、系統分層、角色介面、紫隊技術架構與後續演進方向。

本版本重點不是一次定義完整 Cyber Range，而是建立一套能讓不同工作組平行開發、之後仍可整合的共同架構基線。

目前核心共識：

1. 產品不是單純 Observability Lab，而是 **Gamified Cyber Range / Red-Blue-Purple Attack & Defense Training Platform**。
2. Prometheus + Loki + Grafana（PLG）、Falco、OpenTelemetry / Grafana Alloy 屬於紫隊的 Detection & Telemetry 能力，不是整個產品本身。
3. Grafana 可繼續負責 Dashboard、Query、Alerting，但不應成為 Game State、Score、Exercise State 的核心資料來源。
4. 需要逐步建立一層 **Cyber Range Core / Game Event Layer**，把攻擊事件、防禦事件、評分與 Battleboard 解耦。
5. 網路拓樸是獨立工作項目，目前只定義必要介面，不先決定 VLAN、K8s、VM、Container 等最終形式。
6. AI 將作為後續產品能力導入，但 MVP 不讓 AI 成為核心判定與計分的單一依據。

---

# 2. 產品定位

## 2.1 核心定位

本產品定位為：

> **一套可遊戲化、可觀測、可評分、可重播的資安攻防演練平台。**

平台同時服務四種需求：

- 紅隊：執行攻擊任務、探索目標、取得 Flag / Objective。
- 藍隊：接收 Alert、調查 Incident、執行 Response。
- 紫隊：驗證 Telemetry、Detection、Response 與 ATT&CK Coverage。
- Instructor：控制演練、Reset、調整關卡與檢視全局。

產品不應只像 CTF，也不應只像 SOC Dashboard，而是：

```text
CTF / Cyber Range
        +
SOC Simulation
        +
Detection Engineering
        +
Exercise Evaluation
```

---

# 3. 設計原則

## 3.1 角色與工程模組分離

Red / Blue / Purple 是「演練角色」。

Frontend / Backend / Infra / Scenario / Telemetry 則是「工程工作包」。

兩者不應直接畫上等號。

例如：

```text
Purple Team Role
≠
只負責 Grafana

Purple Platform Workstream
=
Telemetry + Detection + Response + Evaluation
```

---

## 3.2 Grafana 是工具，不是產品中樞

Grafana 適合：

- Metrics / Logs Query
- Dashboard
- Alert Rule
- Contact Point
- Webhook
- SOC 技術視圖

Grafana 不應負責：

- Exercise State
- Round State
- Player / Team State
- Score
- Game Objective State
- Instructor Control
- Battleboard State
- 產品權限模型

因此長期架構應為：

```text
Grafana Alert
    ↓
Cyber Range Core / Event Service
    ↓
Score / Battleboard / Response / Report
```

而不是：

```text
Grafana
├─ Score
├─ Game State
├─ Battleboard State
└─ Instructor State
```

---

## 3.3 Telemetry Schema 與 Game Event Schema 分離

OpenTelemetry 用於統一：

- Logs
- Metrics
- Traces
- Resource attributes
- Service metadata
- Telemetry transport

Cyber Range Event Schema 則是產品 Domain Model，例如：

- attack.detected
- attack.succeeded
- detection.triggered
- response.block
- objective.completed
- score.changed

兩者不可混為同一層。

---

# 4. 產品工作包切分

目前切成 8 個邏輯 Workstream。

| Workstream | 核心責任 |
|---|---|
| 1. Product / Game Design | 遊戲規則、流程、難度、Objective、Hint、UX |
| 2. Scenario / Target | 靶機、漏洞、攻擊鏈、Flag、Scenario Package |
| 3. Blue Operations | Incident、Investigation、Response Workflow |
| 4. Purple Platform | Telemetry、Detection、Response、Evaluation。內部再分 P1 pipeline 與 P2 evaluation，見 [purple_platform_plan.md](./purple_platform_plan.md) |
| 5. Cyber Range Core | Event、Score、Exercise State、API |
| 6. Range Infrastructure | 執行環境、隔離、Reset、網路與部署 |
| 7. Product UI | Player Portal、SOC、Battleboard、Instructor Console |
| 8. Event Control Plane | 憑據、座位、會話；在領號時鑄造 `player_id`，不持有 Exercise state／score |

若目前約 10 人，不建議真的拆成 8 組，可整併為：

```text
2 人：Product + Frontend
2 人：Scenario / Red
2 人：Blue Operations
2 人：Purple Platform
2 人：Platform / Infra
```

跨組共同定義：

- Event Schema
- Scenario Schema
- API Contract
- Score Rules
- Exercise Lifecycle

## 4.1 Workstream 依賴關係

上表是**責任切分**，不是施工順序。八個 workstream 之間有真實依賴，照錯順序做會反覆重工。

```text
            ┌──────────────────────── 地基（所有人都踩在上面）────────────────────────┐
            │  WS6 Range Infrastructure ──→ WS4-P1 Telemetry & Detection（事實生產）  │
            └───────────────┬───────────────────────────────┬────────────────────────┘
                            │                               │
              ┌─────────────┴─────────────┐    ┌────────────┴─────────────┐
              │  技術線（可立刻接續）      │    │  內容／流程線             │
              │  WS4-P2 Evaluation        │    │  WS2 Scenario / Target    │
              │  （判讀：coverage/缺口）   │    │  WS3 Blue Operations      │
              └───────────────────────────┘    └──────────────────────────┘

              產品線（單一依賴鏈，不可跳）
              WS1 Product / Game Design ──→ WS5 Cyber Range Core ──→ WS7 Product UI
                （規則、Objective）          （Exercise State/Score/API）   （四種 Console）

              入場線（50+ 規模，動態配置）
              WS8 Event Control Plane ──→ WS6 Seat Provisioner ──→ WS7 嵌入 shell
```

**三條線與其性質**

| 線 | Workstream | 本質 | 前置條件 |
|---|---|---|---|
| 地基 | WS6 → WS4-P1 | 執行環境與事實生產 | 無（最先做）|
| 技術線 | WS4-P2 | **領域邏輯**：把事實判讀成偵測能力 | WS4-P1 的 Core Event 與 Alert Record |
| 產品線 | WS1 → WS5 → WS7 | 遊戲規則 → 狀態與計分 → 呈現 | WS1 先行，否則 WS5／WS7 會反覆重做 |
| 內容線 | WS2、WS3 | 靶機情境、藍隊工作流 | 需地基就緒才有意義 |
| 入場線 | WS8 → WS6 → WS7 | 憑據／座位／會話 → 動態配置 → 玩家 shell | WS1 的個人計分與 WS5 的 `player_id` schema |

**幾條要記住的約束**

- **WS1 是隱形阻塞。** 它不寫程式，但 WS5 的 Score／Exercise State 與 WS7 的四種 Console
  都由它定義。跳過它直接做 WS5／WS7，等於用猜的規格寫實作。
- **WS4-P2 不必等產品線。** 它的上游是 WS4-P1 的 schema（已定版），與 WS1／WS5 正交，
  可與產品線平行推進。它也是紫隊產品的差異化所在（見 [purple_platform_plan.md](./purple_platform_plan.md) §1.1）。
- **WS7 不該早做。** 它要顯示的數字（coverage、缺口分類、計分）由 WS4-P2 與 WS5 產生；
  數字還不存在時做 UI，只能做出假資料畫面。
- **WS5 是 Core Event 的下游消費者。** WS4-P1 目前把 Core Event 落在自有儲存＋可插拔
  adapter，正是為了等 WS5 就位時能接上而不改 schema（見 §8）。
- **WS2／WS3 依賴地基而非彼此。** 兩者可平行，但都需要 WS6 的環境與 WS4-P1 的遙測才驗得了。

> 本節描述的是**依賴**，不是排程。實際順序仍可依人力調整，但箭頭方向不可逆 ——
> 逆向施工的代價是重工，不是延遲。

## 4.2 Workstream 與六區的對應

§4.1 談的是「誰擋誰」，本節談「誰住在哪」（六區定義見 §12）。兩者是同一組切分的兩個軸，
而且對得出奇地齊 —— 這不是巧合，理由見本節最後三條推論。

**對應不是一對一**：有的 workstream 獨佔一區，有的橫跨兩區，有一個完全沒有分區足跡。

| 線（§4.1）| Workstream | 落在哪一區 | 說明 |
|---|---|---|---|
| 地基 | WS6 Range Infrastructure | **六區全部** | 它不住在任何一區 —— 六區、方向性防火牆與 Seat Provisioner 本身就是它的產出 |
| 地基 | WS4-P1 Telemetry & Detection | **Z-TARGET → Z-MGMT** | collector 在 target 側（契約 4），backend 在 mgmt 側，跨界方向單向（契約 2）|
| 技術線 | WS4-P2 Evaluation & Console | **Z-MGMT → Z-APP** | Evaluation Engine 在 mgmt（coverage／缺口判讀／Alert Record，§8.5）；**Purple Console 在 app**（[purple_platform_plan.md](./purple_platform_plan.md) §3.1）|
| 內容線 | WS2 Scenario / Target | **Z-TARGET**（攻擊來源在 Z-RED）| 靶機、漏洞、Flag 在 target；攻擊鏈起點在 red |
| 內容線 | WS3 Blue Operations | **Z-APP** 操作、**Z-TARGET** 落地 | Blue 只經 SOC Console，**不取得 Z-MGMT 底層存取權**（§12.3）；response 由 agent 在 target 側 pull 執行 |
| 產品線 | WS1 Product / Game Design | **無** | 沒有基礎設施足跡 —— 這正是它成為「隱形阻塞」的原因 |
| 產品線 | WS5 Cyber Range Core | **Z-APP** | G2 把它自 Z-MGMT 遷出的那一個（§12.1）|
| 產品線 | WS7 Product UI | **Z-APP** | Player Portal／Blue SOC／Battleboard／Instructor 四個（**Purple Console 不在此**，見上一列）；Red 走 `RED → APP :443`，不經 Z-MGMT（§12.3）|
| 入場線 | WS8 Event Control Plane | **Z-EDGE → Z-APP**，並反代至 **Z-RED／Z-BLUE** | Z-EDGE 只做 TLS 終結與 ttyd 反代且零持久化；Admission Service／座位池在 Z-APP |

**三條推論**

- **產品線 ＝ Z-APP 線。** WS1 → WS5 → WS7 這條依賴鏈，落地後剛好就是 Z-APP 這一區。
  G2 新增 Z-APP 不是為了多切一個 VLAN，而是 G1 把 Range Core 與 UI 放在 Z-MGMT，
  與 `RED → MGMT deny all` 直接衝突（§12.1）—— **產品線需要一個 Red 打得到的平面**。
- **Z-MGMT 的服務負載只承載觀測與評估。** WS6 的 Seat Provisioner 是 host 側 pull agent，
  不是可由其他區主動呼叫的 Z-MGMT 服務；其他 workstream 若要在此暴露服務，必須先複查契約 3、5。
- **WS4 與 WS8 都跨區，跨界規則必須由契約約束。**
  P1 跨 `TARGET → MGMT`：受紫隊契約 1（三個 port）、2（單向）、4（collector 在 target 側）
  三條約束 —— 五條契約有三條在管這一段。
  P2 跨 `MGMT → APP`：Console 只能拿 Evaluation API，**不得有 raw query 權**
  （[purple_platform_plan.md](./purple_platform_plan.md) §4.1、§3.10），這是 §8.5 那張
  「誰看得到 Alert Record」表在網路層的延伸。
  WS8 跨 `EDGE → APP` 驗證與領號、`EDGE → RED／BLUE :7681` 反代 shell，並受契約 5
  `EDGE → MGMT deny all` 約束。**契約是為跨界而存在的**。

---

# 5. 角色與 UI 分層

## 5.1 Player Portal

Audience：

- Red
- Blue

主要功能：

- Login / Role
- Mission
- Target
- Difficulty
- Hint
- Objective
- Score
- Remaining Time
- Flag Submission

Red Team 不應直接看到 Detection Rule、Threshold、Falco Rule 或封鎖 TTL。

---

## 5.2 Blue SOC Console

Audience：

- Blue
- 部分 Purple

主要功能：

- Alert
- Incident Queue
- Evidence
- Source / Target
- Timeline
- Response Action
- Resolve Incident

底層可使用 Grafana 作為 SOC 技術視圖，但產品 UI 可再包一層較簡化的 Incident UX。

---

## 5.3 Purple Analysis Console

Audience：

- Purple
- Instructor

主要功能：

- Telemetry Coverage
- Detection Hit / Miss
- ATT&CK Mapping
- Detection Latency
- Response Latency
- MTTD
- MTTR
- ~~Detection Rate~~ → 改用 `action coverage`
- Containment Rate
- Rule Evaluation
- Coverage Gap

> **命名定案**（[ADR 0001](./docs/adr/0001-p1-output-contract.md) ⑨）：
> `Detection Rate` 已廢除，只留 `action coverage`。前者沒有說分母是什麼，
> 後者的名字裡就寫著分母是「動作」。本文件與 discuss.md 的 `Detection Rate` 一律視為歷史別名。
>
> **MTTR 定義**（ADR ⑦）：終點是 **Response action 生效**，不是 Grafana Resolved。
> 後者測的是攻擊停了沒，與藍隊做了什麼無關，另外命名為 `containment duration`。

畫面規格與下鑽結構見 [purple_platform_plan.md](./purple_platform_plan.md) §3.7。

Purple 的核心目標不是「多裝幾套工具」，而是驗證：

```text
Attack
  ↓
Telemetry
  ↓
Detection
  ↓
Correlation
  ↓
Response
  ↓
Verification
```

---

## 5.4 Live Battleboard

Audience：

- Red
- Blue
- Purple
- Instructor
- 教室 / 大螢幕

定位：

> **Battleboard 是觀賽介面，不是操作介面。**

主要 Widget：

1. Match Header
2. Red / Blue Score
3. Round Timer
4. Attack Chain Progress
5. Scenario-based MITRE ATT&CK
6. Defense Status
7. Live Timeline
8. Objective Progress

Battleboard 只展示 Sanitized Event。

不得公開：

- Rule threshold
- Raw payload
- Detection query
- Secret
- Ban TTL
- Internal IP mapping
- Falco rule detail

資料流：

```text
Raw Security Event
      ↓
Normalization
      ↓
Sanitization
      ↓
Public Battle Event
```

---

## 5.5 Instructor Console

Audience：

- Instructor

主要功能：

- Start
- Pause
- Reset
- Stop
- Scenario Select
- Difficulty
- Inject Hint
- Inject Event
- Override Score
- View All Teams
- View Raw Event
- Download Exercise Report

Instructor 可能是實際商業產品最重要的客戶角色之一。

---

# 6. Purple Platform 架構

## 6.1 技術選型

目前建議：

### Collection / Normalization

- OpenTelemetry
- Grafana Alloy

### Telemetry Backend

- Prometheus
- Loki

### Runtime Sensor

- Falco —— **sensor，不是 alert engine**

Falco 的偵測發生在 syscall 層，Grafana 看不到 syscall，因此「單一 alert engine」結構上做不到。
但 Falco 事件一律先寫入 Loki，再由 Grafana rule 決定是否構成告警：

```text
Falco ──→ Alloy ──→ Loki ──→ Grafana Alerting ──→ Event Service
 事實                              判斷
```

這樣才存在「telemetry 有、detection 沒有」的中間狀態，
[purple_platform_plan.md](./purple_platform_plan.md) §3.4 的偵測缺口才可觀測。
例外（次秒級自動圍堵）須在 scenario 定義中明寫。見 [ADR 0001](./docs/adr/0001-p1-output-contract.md) ③。

### Detection

- Grafana Alerting —— **唯一 alert engine**，`eval interval` = 10s
- PromQL
- LogQL

### Response

- Webhook
- Response Controller
- MVP 可使用 ipset / iptables

### Visualization

- Grafana：SOC / Technical View
- 自製 Web UI：Battleboard / Product UI

---

## 6.2 PLG 定義

本文件中的 PLG：

```text
P = Prometheus
L = Loki
G = Grafana
```

PLG 負責：

```text
Metrics
Logs
Query
Dashboard
Alert
```

Falco 則補足：

```text
Runtime Security
Syscall
Process
Container Activity
Sensitive File Access
Shell Spawn
```

---

## 6.3 OpenTelemetry / Alloy

OTel 不只用來「統一 Log Format」。

更準確的定位：

> **統一 Telemetry Data Model + Collection / Transport Layer**

建議：

```text
Application / Host / Container
             ↓
      OTel SDK / Agent
             ↓
       Grafana Alloy
        ↙         ↘
      Loki      Prometheus
```

未來若導入 Tracing：

```text
Alloy
  ├─ Loki
  ├─ Prometheus
  └─ Tempo
```

MVP 不建議同時疊：

```text
OTel Collector
    ↓
Alloy
    ↓
Backend
```

除非未來有明確 Gateway / Agent 雙層需求。

---

# 7. Detection & Response Flow

目前已完成的 SQLi Workshop 可視為第一個完整 Scenario Prototype：

```text
SQL Injection
     ↓
Application Structured Log
     ↓
Alloy
     ↓
Loki
     ↓
Grafana Alert Rule
     ↓
Webhook
     ↓
Alert Receiver
     ↓
ipset / iptables
     ↓
Source Blocked
```

這條鏈路證明：

```text
Detect → Alert → Respond → Verify
```

可運作。

未來不要只靠 Application Log。

可增加：

### Scenario 01

```text
SQL Injection
Loki → Grafana → Response
```

### Scenario 02

```text
Container Shell
Falco → Loki → Grafana → Response
```

### Scenario 03

```text
Sensitive File Access
Falco → Loki → Grafana → Incident
```

> 早期草案寫的是 `Falco → Event → ...`（Falco 直送）。已於
> [ADR 0001](./docs/adr/0001-p1-output-contract.md) ③ 改為經 Loki 與 Grafana，
> Falco 定位為 runtime sensor。

### Scenario 04

```text
Brute Force
Logs / Metrics → Detection → Block
```

---

# 8. Cyber Range Core

## 8.1 為何需要

隨著系統擴大，會遇到：

```text
誰知道 Red 成功？
誰知道 Blue 偵測成功？
誰負責加分？
誰通知 Battleboard？
誰計算 MTTD？
誰保存 Exercise State？
```

這些問題不應交給 Grafana。

因此建立：

> **Cyber Range Core**

---

## 8.2 核心模組

```text
Cyber Range Core
│
├─ Event Service
├─ Exercise Manager
├─ Score Engine
├─ Scenario Registry
├─ Response Orchestrator
└─ Report Generator
```

---

## 8.3 Event Service

輸入來源：

- Grafana Webhook —— **偵測事件的唯一入口**
- Target App
- Blue Action
- Instructor Action
- Future AI Agent
- Future Network Sensor

> Falco **不是**直接輸入來源。它是 runtime sensor，事件經 Loki → Grafana Alerting 進來
> （§6.1、[ADR 0001](./docs/adr/0001-p1-output-contract.md) ③）。
> §7 Scenario 02／03 原本寫的 `Falco → Event` 已作廢。

輸出：

- Battleboard
- Score Engine
- Purple Console
- Instructor Console
- Report

---

## 8.4 Event Schema 草案

```json
{
  "event_id": "evt-123",
  "exercise_id": "ex-001",
  "scenario_id": "sqli-01",
  "event_type": "attack.detected",
  "lifecycle": "firing",
  "severity": "high",
  "source": "grafana",
  "team": "red",
  "technique": "T1190",
  "target": {
    "service": "vulnerable-app"
  },
  "observed_at": "2026-08-07T14:30:00+08:00",
  "visibility": "purple"
}
```

Visibility：

```text
public
red
blue
purple
instructor
```

由 P1 Webhook Receiver 依 `event_type` 對照表決定，Grafana rule 不得覆寫
（[ADR 0001](./docs/adr/0001-p1-output-contract.md) ⑪）。

Lifecycle：

```text
firing     ← MTTD 的終點
resolved   ← containment duration 的終點
```

`pending` 不產生 Core event。同一告警的 `firing` 與 `resolved` 共用同一個 `event_id`（ADR ⑥）。

## 8.5 為什麼沒有 `evidence_ref`

早期草案帶有 `evidence_ref: "loki://..."`，**已移除**。它讓 Core 的 domain model 知道
log backend 是 Loki，直接違反 §3.3；而且 Core 永遠不會讀這個欄位。

證據仍然查得到，只是住在另一份紀錄：

| | 住哪 | 內容 | 誰讀 |
|---|---|---|---|
| **Core Event**（本節） | Cyber Range Core | 遊戲語意 | Battleboard／Score／Console |
| **P1 Alert Record** | Z-MGMT，P1 擁有 | Grafana rule、原始 query、觸發值、backend | 只有 Evaluation Engine |

兩者用 `event_id` 對接，由 P1 Webhook Receiver 統一鑄造。
Console 下鑽走 `GET /evidence/{event_id}`，由 Engine 代取，防火牆政策不變。
換掉 Loki 時本節 schema 一個字都不動。見 [ADR 0001](./docs/adr/0001-p1-output-contract.md) ①②④。

---

# 9. Score Engine

MVP 不追求複雜。

可以先採 Objective-based Score。

Red：

```text
Discover Target       +50
Initial Access       +100
Privilege Escalation +200
Capture Flag         +500
```

Blue：

```text
Detect Attack        +100
Identify Technique   +100
Contain < 60 sec     +150
Resolve Incident     +100
```

Purple：

Purple 不一定直接參與紅藍比分，可改成 Exercise Quality KPI：

```text
Detection Coverage
MTTD
MTTR
False Positive
Containment Rate
ATT&CK Coverage
```

---

# 10. MITRE ATT&CK 設計

不建議在 Battleboard 直接放完整 Enterprise Matrix。

應使用：

> **Scenario-based ATT&CK**

例如 SQLi Scenario：

```text
Initial Access
└─ T1190

Execution
└─ Related Web Exploitation

Credential Access
└─ Optional

Collection
└─ Optional

Exfiltration
└─ Optional
```

狀態：

```text
○ Not Reached
🟡 Attempted
🔴 Red Success
🟢 Blue Detected / Blocked
```

Purple Console 可呈現完整 technique 與 coverage。

Battleboard 只呈現必要技術與戰況。

---

# 11. Scenario Package

每個 Scenario 應逐步標準化。

草案：

```yaml
id: sqli-01
name: SQL Injection
difficulty: easy

objectives:
  - discover_endpoint
  - trigger_sqli
  - capture_flag

attack_mapping:
  - T1190

target:
  type: web
  service: vulnerable-app

telemetry:
  - app_log
  - http_metric

detection:
  - SQLInjectionBurst

duration: 30m
```

未來可讓 Scenario 變成：

```text
Scenario Package
├─ metadata.yaml
├─ target/
├─ detection/
├─ scoring/
├─ hints/
└─ reset/
```

---

# 12. Range Infrastructure / 網路拓樸

## 12.1 現行拓樸與世代關係

現行架構拓樸只有一張：[demo_network_topology_v0_3.svg](./demo_network_topology_v0_3.svg)
（G3 六區＋中控，已納入 #65 決策）。G2 四區是已實作基線；G3 的動態座位與 Z-BLUE
受限環境已由 #62 落地（PR #116／#117／#118，真環境驗證於 10.167.223.45）。

**「拓樸不定案」的正確意思不是「什麼都沒決定」，而是「明確知道哪幾件事一旦變動要重做」。**
不定案指的是實作形式（VM／Container／K8s 比例、Overlay、SDN、Cloud VPC），
不是指介面——介面已經定了，見 §12.2。

歷代拓樸與退場理由記錄在 [archive/README.md](./archive/README.md)：

| 世代 | 圖 | 狀態 | 退場理由 |
|---|---|---|---|
| G0 | kali_nat_webserver | 已歸檔 | 無觀測平面；NAT 讓六台 kali 的來源 IP 塌縮成兩個主機 IP，事件無法歸屬到個別攻擊者 |
| G1 | v0_2 | 已歸檔 | Battleboard／Portal／Range Core 放在 Z-MGMT，與同圖的 `RED → MGMT: deny all` 直接衝突 |
| G2 | v0_2_1 | 已實作基線 | 四區；尚無會議中控、動態座位與藍隊受限 shell 區 |
| **G3** | **六區＋中控** | **現行架構** | 新增 Z-EDGE（VLAN 50）、Z-BLUE（VLAN 60）與 WS8；#20（網路契約）與 #62（座位 runtime）均已交付 |
| G4 | v3_k8s | 之後擴展考量 | 尚未施工。已定調「靶機與攻擊機不加入 cluster」，見 §12.4 |

G2 相對 G1 的唯一結構變更：**新增 Z-APP（VLAN 40）產品平面**，
把 Range Core 與所有 Product UI 自 Z-MGMT 遷出。Z-MGMT 從此只承載觀測與評估。

G3 相對 G2 新增 **Z-EDGE（VLAN 50）**會議中控入口與 **Z-BLUE（VLAN 60）**防守平面。
Z-EDGE 零持久化；Admission Service 與座位池留在 Z-APP。Z-BLUE 提供受限 shell，且不取代
無玩家 shell 的 Z-TARGET 遙測基準線。

## 12.2 跨世代契約（五條）

前四條自 G1 起未曾變動；G3 新增第五條。它們是「拓樸可退可換、Purple Platform 不必重做」的依據。
**變更任何一條都要視為破壞性變更**，必須同步檢查 Purple Platform 全層。

| # | 契約 | 破壞後果 |
|---|---|---|
| 1 | telemetry 只走三個 port：`:3100` Loki ／ `:9090` Prometheus ／ `:4317` OTLP gRPC | 收集端要重寫 |
| 2 | `TARGET → MGMT` **單向**；MGMT 不主動連 target | Response 路徑要重新設計 |
| 3 | `RED → MGMT` **deny all** | 觀測平面變成攻擊面，涵蓋率數字失去公信力 |
| 4 | collector（Alloy／Falco／response agent）裝在 **target 側**，不在 mgmt 側 | 部署模型與 Falco 的 syscall 可見度都要重做 |
| 5 | `EDGE → MGMT` **deny all** | 匿名網際網路入口可觸及觀測平面；入口被攻陷會污染量測可信度 |

契約 1 的三個 port 是 **telemetry allowlist**，不是 response control traffic。
契約 2 的 agent-pull 實作另有一條最小權限例外：只允許 `Z-TARGET → 指定的
MGMT response receiver :8000/TCP`。`:8000` 不得放行到其他 Z-MGMT 主機，其他
`TARGET → MGMT` port 仍由防火牆封鎖。

第 2 條是 Response 採 **agent pull 模式**的原因：
若改用 MGMT 主動下指令（push），單向性即被打破，MGMT 會成為可主動打 target 的來源。
延遲換取單向性，這是刻意的取捨。

## 12.3 G3 六區必須支援的能力

- Red 可存取指定 Target（`RED → TARGET :80 :3306`）
- Red 可存取 Player Portal 與 Battleboard（`RED → APP :443`）——**不得經由 Z-MGMT**
- Purple 可取得 Telemetry（§12.2 契約 1、2）
- Blue 透過 Blue SOC Console 操作，不取得 Z-MGMT 底層存取權
- Management Plane 與 Attack Surface 隔離（§12.2 契約 3）
- Scenario 可以 Reset
- 不同演練環境之間可避免互相污染
- Response 不應誤傷 Host／管理網路（§12.2 契約 2、4）
- Internet 只由 Z-EDGE `:443` 進入；Z-EDGE 不持久化，也不得觸及 Z-MGMT／Z-TARGET
- Z-EDGE 可經 `:7681` 反代至 Z-RED／Z-BLUE 各 seat 內的 ttyd；ttyd 不得跑在 host mgmt NIC
- Z-BLUE 玩家只有受限 shell；sudo allowlist 不得包含 Falco／Alloy 設定、unit 或停用路徑

## 12.4 仍未決定

- VM vs Container 比例
- Overlay Network／SDN／Service Mesh／Multi-Cluster
- Cloud VPC 細節
- 每場演練是否需要獨立 VLAN pair（G4 的 per-tenant 模式）
- 六區在 50+ seat、可能多主機承載下的實體網路配置

**已不再是未決事項**：邏輯區數（G3 定為六區）、是否全面 K8s（否——G4 已定調
k8s 只承載控制平面與觀測堆疊，靶機與攻擊機不加入 cluster，
使 `kubelet :10250` 與 `kube-apiserver :6443` 永不出現在攻擊面）。

---

# 13. AI 輔助方向

AI 為後續產品重要能力，但應採「Assistant」而不是「核心裁判」的原則。

## 13.1 第一階段：AI Assistant

建議優先做低風險輔助：

### Red

- Hint Generation
- Attack Concept Explanation
- Scenario Guidance

### Blue

- Alert Summary
- Incident Timeline Summary
- Evidence Explanation
- Next-step Recommendation

### Purple

- Detection Gap Summary
- Falco / LogQL / PromQL Rule Explanation
- ATT&CK Mapping Suggestion
- MTTD / MTTR Analysis
- Post-Exercise Recommendation

### Instructor

- Exercise Summary
- Automatic Report Draft
- Scenario Difficulty Suggestion
- Player Progress Summary

---

## 13.2 第二階段：AI Copilot

```text
Event Service
    ↓
AI Context Builder
    ↓
LLM
    ↓
Suggested Action
```

AI 可建議：

- Block
- Investigate
- Add Detection Rule
- Add Hint
- Adjust Difficulty

但需要 Human-in-the-loop。

---

## 13.3 第三階段：AI-driven Exercise

未來可探索：

- Dynamic Scenario
- AI Red Agent
- AI Blue Assistant
- Adaptive Hint
- Adaptive Difficulty
- Automatic Purple Team Review
- Automated Debrief
- Exercise Replay Explanation

但 **Score / Flag / Objective 判定仍應由 deterministic system 為主**。

避免：

```text
LLM 說「應該成功」
→ 直接加分
```

正確方式：

```text
Telemetry / Flag / State
→ Deterministic Validation
→ Score

AI
→ Explain / Recommend / Summarize
```

---

# 14. 系統總體邏輯架構

```text
                          ┌──────────────────────┐
                          │    Product Web UI    │
                          │                      │
                          │ Player / Blue /      │
                          │ Battle / Instructor  │
                          └──────────┬───────────┘
                                     │
                                     ▼
                          ┌──────────────────────┐
                          │   Cyber Range Core   │
                          │                      │
                          │ Event / Exercise /   │
                          │ Score / Scenario     │
                          └───┬─────────┬────────┘
                              │         │
                 ┌────────────┘         └─────────────┐
                 ▼                                    ▼
        ┌─────────────────┐                  ┌─────────────────┐
        │ Purple Platform │                  │ Scenario / Range │
        │                 │                  │                  │
        │ OTel / Alloy    │                  │ Vulnerable Apps  │
        │ Prometheus      │                  │ Linux / Windows  │
        │ Loki            │                  │ Container / VM   │
        │ Grafana         │                  │ Future K8s       │
        │ Falco           │                  └────────┬─────────┘
        └──────┬──────────┘                           │
               │                                      │
               └──────────── Telemetry ───────────────┘
                              │
                              ▼
                       Detection / Event
                              │
                              ▼
                     Response / Evaluation
                              │
                              ▼
                        Cyber Range Core
                              │
                 ┌────────────┼────────────┐
                 ▼            ▼            ▼
              Score       Battleboard    Report
```

---

# 15. 建議 API Boundary

MVP 可先保留：

```text
GET  /api/exercises/current
GET  /api/events
GET  /api/incidents
GET  /api/score
GET  /api/scenarios

POST /api/events
POST /api/actions
POST /api/exercises/start
POST /api/exercises/reset
```

Realtime：

```text
WS /api/events/live
```

或先使用：

```text
SSE /api/events/live
```

---

# 16. MVP → V2 → V3

## MVP

目標：

> 先做出一個完整、可展示、能自動驗證的攻防閉環。

建議：

- SQLi Scenario
- Container Runtime Scenario
- PLG
- Falco
- OTel / Alloy
- Grafana Alerting
- Event Service MVP
- Simple Score
- Battleboard MVP
- Player Portal MVP
- Basic Reset
- Manual / Semi-auto Instructor Control

MVP Battleboard：

- Red / Blue Score
- Round Timer
- Attack Chain
- Latest Events
- Current Defense Status

---

## V2

增加：

- Multiple Scenarios
- MITRE ATT&CK Mapping
- MTTD / MTTR
- Action Coverage
- Incident Workflow
- WebSocket / SSE
- Scenario Registry
- Report Generator
- Better RBAC
- AI Summary
- AI Hint
- AI Purple Analysis

---

## V3

完整平台化：

- Multi-Team
- Multi-Scenario
- Multi-Tenant
- Replay
- Advanced Instructor Console
- Dynamic Scoring
- Scenario Marketplace / Package
- K8s 控制平面 + per-tenant range（靶機與攻擊機**不加入** cluster，見 §12.4）
- AI Copilot
- Adaptive Exercise
- Automated Debrief
- AI Purple-Team Coach

---

# 17. v0.1 非目標

目前不處理：

- 完整 SIEM 替換
- 大規模 Enterprise SOC
- 複雜 SOAR
- 完整 K8s Multi-Cluster
- Cloud Multi-Region
- 大量 Network Appliance
- 完整 ATT&CK Matrix
- AI 自主裁判
- AI 自主執行高風險 Response
- Production-grade Multi-Tenancy

目標是先證明：

```text
Attack
→ Telemetry
→ Detection
→ Event
→ Response
→ Score
→ Visualization
→ Evaluation
```

---

# 18. Open Questions

以下留待後續決策：

### Product

- 最終產品名稱
- CTF 與 Cyber Range 的比例
- 是否讓 Purple 參與比分
- Difficulty 如何影響 Hint / Detection / Target

### Core

- PostgreSQL 是否 MVP 就導入
- Redis / PubSub 何時需要
- SSE vs WebSocket
- Event Retention

### Purple

- Falco Event 是否直接送 Event Service
- Grafana Alert 是否作為主要 Alert Engine
- 是否導入 Tempo
- 是否需要 Wazuh / Suricata / Zeek / Tetragon

### Range

- Docker / VM / K8s 比例
- Network segmentation
- Reset mechanism
- Per-team isolation

### AI

- AI Provider
- Local Model vs API
- Context / RAG
- Exercise Data Privacy
- Human Approval Boundary

---

# 19. v0.1 結論

目前最合理的產品結構不是：

```text
Red Team
Blue Team
Purple Team
```

三組工具各自堆疊。

而是：

```text
Cyber Range Product
│
├─ Game / Product Layer
├─ Scenario Layer
├─ Blue Operations
├─ Purple Detection Platform
├─ Cyber Range Core
├─ Range Infrastructure
└─ Product UI
```

其中 Purple Platform 可以先獨立完成：

```text
OTel / Alloy
    ↓
Prometheus / Loki
    ↓
Grafana Detection
    +
Falco Runtime Detection
    ↓
Event Service
    ↓
Response / Evaluation
```

而整個產品真正的中樞逐步轉向：

> **Cyber Range Core + Game Event Layer**

這會讓未來的 Battleboard、Score、Instructor、AI、Scenario、Report 都能建立在一致的 Event Model 上，而不是彼此直接耦合 Grafana、Loki、Falco 或底層網路工具。

v0.1 的策略應維持：

> **先做出一個完整閉環，再逐步平台化；網路拓樸先保留彈性；AI 後置導入並採 Human-in-the-loop。**
